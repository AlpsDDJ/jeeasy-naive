export const enum FormType {
  // 新增
  ADD = 'add',
  // 修改
  EDIT = 'edit',
  // 查看
  VIEW = 'view'
}

export const enum FormDataType {
  STRING,
  NUMBER,
  BOOLEAN,
  DATE,
  TIME,
  DATETIME
}

export const enum InputType {
  INPUT,
  INPUT_NUMBER,
  DATE,
  TIME,
  DATETIME,
  SWITCH,
  SELECT,
  TREE_SELECT,
  DICT_SELECT,
  SYS_USER_SELECT
}
