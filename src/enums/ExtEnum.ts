export const enum FormType {
  ADD,
  EDIT,
  VIEW
}

export const enum FormDataType {
  STRING,
  NUMBER,
  BOOLEAN,
  DATE,
  TIME,
  DATETIME
}

export const enum InputType {
  INPUT,
  INPUT_NUMBER,
  DATE,
  TIME,
  DATETIME,
  SWITCH,
  SELECT,
  TREE_SELECT,
  DICT_SELECT,
  SYS_USER_SELECT
}
